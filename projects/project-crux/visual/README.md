# Visual Design output — project-crux

Output destination for the Visual Design track (and Brand Identity, where the two overlap — e.g. color/type/logo work), per the handoff packet(s) in `projects/project-crux/handoff/`.

Generated by GPT-Sol (project-level model override — see `charter.md`'s Model assignment table). Each drop should say which handoff packet it's answering and the date, so it's traceable back to the instruction that produced it.

Advisory review of anything here (Critic, on Grok) lands as its own file, clearly labeled as critique — not merged into or overwriting the design files it's reviewing. David decides what's Done, per `charter.md`'s Done Decider checklist for this track.
